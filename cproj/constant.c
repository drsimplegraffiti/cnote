#include <stdio.h>

int main() {
  const int DAYS_IN_WEEK = 7; // Constant integer
  const float PI = 3.14159f;  // Constant float
  const char NEWLINE = '\n';  // Character constant

  printf("Days in a week: %d%c", DAYS_IN_WEEK, NEWLINE);
  printf("Value of PI: %.2f\n", PI);
  return 0;
}
