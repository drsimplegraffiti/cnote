#include <stdio.h>

int main(void) {
  // legal name
  int counter;
  float average_score;

  // not recommended
  int x1;
  int _temp;

  counter = 5;
  average_score = 90.5;

  printf("Counter = %d, Average Score = %.1f\n", counter, average_score);
}
